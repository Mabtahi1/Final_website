version: '3.8'

services:
  prolexis-platform:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=true
      - PORT=5000
      - SECRET_KEY=dev-secret-key-change-in-production
      - AWS_DEFAULT_REGION=us-east-1
      # Add your actual keys here for local testing
      # - STRIPE_SECRET_KEY=sk_test_...
      # - STRIPE_PUBLISHABLE_KEY=pk_test_...
    volumes:
      - ./uploads:/app/uploads
      - ./logs:/app/logs
      - ./templates:/app/templates
      - ./static:/app/static
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  uploads:
  logs: